export function ConverterView() {
  return (
    <section className="content-panel" aria-labelledby="converter-heading">
      <p className="eyebrow">Converter</p>
      <h2 id="converter-heading">Convert files locally.</h2>
      <p>
        Image, spreadsheet, media, PDF, Markdown, and office conversion flows
        will appear here as each local backend is added.
      </p>
      <div className="empty-card">Conversion backends are not configured yet.</div>
    </section>
  );
}
